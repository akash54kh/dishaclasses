#include<iostream.h>
#include<conio.h>
void main()
{
	int year;
	clrscr();

	cout<<"Enter a year: ";
	cin>>year;

	if (year%100==0)
	{
		if (year%400==0)
		{
			flag = 1
		}

		else
		{
			flag = 0
		}
	}

	if (year%4==0)
	{
